const sectionHeading = (title, subTitle) => {
  return (
    <div className="text-left mt-24">
      <h2 className="text-6xl text-[#20284F] py-3">{title}</h2>
      <p className="py-3">{subTitle}</p>
    </div>
  );
};

export default sectionHeading;
